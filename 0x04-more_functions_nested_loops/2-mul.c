#include "main.h"
#include <stdio.h>

/**
 * mul- multiplies two integers
 * @a:- the first integers
 * @b:- the second integers
 * Return: always 0
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
	_putchar('\n');
}
